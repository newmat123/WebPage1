var container = document.getElementsByClassName()

function f() {
  var card = document.createElement("div");
  var node = document.createTextClass("card");
  card.appendChild(node);
  container.appendChild(card);
}
